%h2 Record management
= link_to 'New Patient Record', new_patient_path
|
= link_to 'Patient Assessments', unassigned_assessments_path
%br
%br
%table#patients{:data => {:source => [patients_url(format: "json") ]}, :class => "table table-striped table-bordered" }
  %thead
    %tr
      %th Surname
      %th First Name
      %th Date of birth
      %th Anesthetist
      %th Surgeon
      %th Procedure date
      %th Printable summary
    %tbody
      -#- @patients.each do |patient|
      -#  %tr
      -#    %td #{link_to patient.name, patient}
      -#    %td #{patient.dob}
      -#    %td #{patient.anesthetist.name if !patient.anesthetist.nil?}
      -#    %td #{patient.surgeon}
      -#    %td #{patient.planned_date_of_surgery}
      -#    %td no
-#= will_paginate