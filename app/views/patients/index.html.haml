%h2 Patients
%table#patients{:data => {:source => [patients_url(format: "json") ]}, :class => "table table-striped table-bordered" }
  %thead
    %tr
      %th Surname
      %th First Name
      %th Date of birth
      %th Anesthetist
      %th Surgeon
      %th Planned date of Procedure
      %th Show Summary
    %tbody
      -#- @patients.each do |patient|
      -#  %tr
      -#    %td #{link_to patient.name, patient}
      -#    %td #{patient.dob}
      -#    %td #{patient.anesthetist.name if !patient.anesthetist.nil?}
      -#    %td #{patient.surgeon}
      -#    %td #{patient.planned_date_of_surgery}
      -#    %td no
-#= will_paginate
%br
= link_to 'New Patient', new_patient_path
