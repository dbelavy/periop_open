%h1 New assessment
= render 'patients/nav'
- if !@patient.nil?
  =render 'patients/detailed_info'
#form_placeholder.loading
  -#%h2.centered loading
= render 'patients/nav'

:javascript
  loadData = function (){
  var url = decodeURIComponent($(document).getUrlParam('q'))
  $.get(url, function(data) {data;
  window.checkAllDocument();
  $('#form_placeholder').removeClass('loading')
  },"script");

  }
  loadData();
