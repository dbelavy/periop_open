-if !@patient.nil?
  = render('patients/short_info')
  -if @assessment.form.patient_form?
    = render 'assign_form'
    = link_to 'Delete assessment', @assessment, :confirm => 'Are you sure?', :method => :delete,:class=> "btn btn-danger"
= render 'form', :form_disabled => true
= render 'patients/nav'