%h2 Preoperative summary
%table#summary{:class => "table table-striped table-bordered" }
  %thead
    %tr
      %th Concept
      %th Answer
      %th Details
      %th Author
      %th Date
      %th Assessment name
    %tbody
    - @concepts.each do |c|
      - if !@summary[c._id].nil?
        - @summary[c._id].each do |sum|
          %tr
            %td
              =c.display_name
            %td
              =sum[:answer].to_s
            %td
              =sum[:details]
            %td
              =sum[:author]
            %td
              =sum[:date]
            %td
              =sum[:assessment_name]
= link_to 'Back', patients_path