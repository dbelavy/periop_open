%h3 Doctors list
- @doctors.each do |doctor|
  %p Doctor: #{link_to doctor.name + ' | ' + user.role, doctor}


- if signed_in?
  %p Loggged in as: #{current_user.name + ' Role: ' +  current_user.role}
- else
  %p not signed in TODO
